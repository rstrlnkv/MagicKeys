# Сборка
MagicKeys.exe
*.pdb
BuildInfo.g.cs

# Установщик: пакеты и подтянутый набор WiX
*.msi
*.wixpdb
/installer/obj/
/installer/wix/

# Драйверы Apple: несвободные файлы, публиковать нельзя — см. drivers/ЧИТАЙ.md
drivers/

# Мусор
*.tmp
*.log

# Приватное лежит в .backstage, а сюда приходит ссылками: папки — соединением,
# файлы — жёсткой ссылкой (настоящие символьные на Windows требуют особых прав).
# Игнорируются именно ссылки, иначе git видел бы одно и то же дважды и предлагал
# добавить содержимое приватного репозитория в открытый.
/.backstage/
/CLAUDE.md
/ARCHITECTURE.md
/docs
/.claude/
/plugins/
/.claude-plugin/

# Рабочие файлы разбора и замеров
/tools/preview.png
MagicKeys.Tests.exe
